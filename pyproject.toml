[project]
name = "nikke"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.3.0",
]

[project.scripts]
extract = "nikke:extract"
generate = "nikke:generate"
bundle = "nikke:bundle"
package = "nikke:package"
install = "nikke:install"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
